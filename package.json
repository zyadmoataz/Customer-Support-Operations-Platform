{
  "name": "customer-support-operations-platform",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node scripts/build-all.mjs",
    "build:react": "npm --prefix customer-portal run build",
    "build:angular": "npm --prefix support-workspace run build"
  }
}
